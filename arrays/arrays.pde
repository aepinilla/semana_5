// Declarar el array
float [] miArray;
int espacios = 100;

// Inicializamos
miArray = new float[espacios];

//  variable; condicion; incremento
for(int i = 0; i < 100; i++){
  miArray[i] = random(10);
  println(miArray[i]);
}

//
